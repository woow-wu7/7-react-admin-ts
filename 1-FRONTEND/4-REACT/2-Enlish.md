# English

```
reference 参考文献 引用
usage 使用 用途
-
mutable 可变的
caveat 注意事项 警告
-

algorithm 算法
// This algorithm has high application value. 该算法具有较高的应用价值

be aware of. 知道 意识到 注意
plain 普通的 平原
plain sailing. 一帆风顺
// React is not aware of when you change it because a ref is a plain javascript object.
// React不知道您何时更改它，因为ref是一个普通的JavaScript对象

unpredictable 无法预测的
unpredictable results. 不可预知的结果
// Do not write or read ref.current during rendering, except for initialization.
// This makes your component’s behavior unpredictable.
// 除了 初始化 外不要在渲染期间写入或者读取 ref.current，否则会使组件行为变得不可预测。

strict 严格的 adj
// strict mode. 严格模式

discard 放弃v 丢弃n
// but one of the versions will be discarded. 但是其中一个版本将被丢弃

pure 纯的
// If your component function is pure (as it should be), this should not affect the behavior.
// 如果使用的是组件纯函数（也应当如此），那么这不会影响其行为

accidental 意外的adj 偶然n
// He can face the accidental trouble. 他能够面对意外的困难

impurity 杂质
// A filter will remove most impurities found in water. 过滤器会滤掉水中的大部分杂质
```
